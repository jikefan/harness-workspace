# Templates

Reusable scaffolds for profiles, tasks, and repo-local harness files.

- `profile/`: YAML templates for new project profiles.
- `task/`: task workspace and task registry templates.
- `repo/`: repo-local `AGENTS.md` and `scripts/agent/check.sh` templates.

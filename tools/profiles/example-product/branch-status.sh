#!/usr/bin/env bash
set -euo pipefail

cat <<'MSG'
Example branch-status tool

This is an anonymized placeholder. In a real profile, replace this with a read-only
script that compares important branches across the project's repositories.
MSG

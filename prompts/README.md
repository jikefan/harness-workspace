# Prompt Templates

This directory holds reusable prompt templates for repeatable, high-risk coding
work.

Use them as starting points, not mandatory wrappers. Projects may define
narrower versions under `profiles/<project>/prompts/`.
